<!DOCTYPE html>
<html>
  <body>
    <h1>Titles</h1>
      <% @articles.each do |article| %>
        <a href="/articles/<%= article.id %>">
          <%= article.title %>
          <%= articles.content %>
        </a><br>
      <% end %>
  </body>
</html>

<!--Iterate over each song in the @articles collection.
Render a link pointing to the song’s show route/URL with
the ID as the identifier in the URL and the song’s name as the link text.
 -->
